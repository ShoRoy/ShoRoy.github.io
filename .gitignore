/_site/
/.quarto/
.DS_Store
Thumbs.db
**/*.quarto_ipynb
/.github/agents/

# Article PDFs dropped into assets/ for figure extraction only.
# The downloadable resume is tracked explicitly.
assets/*.pdf
!assets/Shobhan_Roy_Resume.pdf

# Source-format media (only the web-encoded variants are shipped)
assets/*.avi
assets/*.svg
assets/Highres_crystal.png
