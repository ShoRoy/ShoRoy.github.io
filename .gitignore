/_site/
/.quarto/
notes/
.DS_Store
Thumbs.db
**/*.quarto_ipynb
/.github/agents/

# Local agent definitions contain private positioning notes / references —
# never publish to the public repo. Kept on disk for VS Code workspace-agent
# discovery only.
/.claude/

# Article PDFs dropped into assets/ for figure extraction only.
# The downloadable resume is tracked explicitly.
assets/*.pdf
!assets/Resume.pdf

# Source-format media (only the web-encoded variants are shipped)
assets/*.avi
assets/*.svg
assets/Highres_crystal.png
